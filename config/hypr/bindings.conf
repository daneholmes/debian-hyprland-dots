# Application bindings
# Terminal
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(hl-cmd-terminal-cwd)"
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(hl-cmd-terminal-cwd)" tmux new
# Browser
bindd = SUPER SHIFT, RETURN, Browser, exec, hl-launch-browser
bindd = SUPER SHIFT ALT, RETURN, Browser (private), exec, hl-launch-browser --private
# File explorer
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- thunar --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- thunar --new-window "$(hl-cmd-terminal-cwd)"
