# Changes require a restart to take effect.

# Ensure bins are in the path
export META_PATH="$HOME/.local/usr/hypeless"
export PATH="$HOME/.local/bin:$PATH"

. "$HOME/.config/uwsm/default"

hl-cmd-present mise && eval "$(mise activate bash)"